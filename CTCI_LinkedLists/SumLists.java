package CTCI_LinkedLists;

public class SumLists {
    // backwards order: easy just carry the carry values

    // forward order: harder. recursive solution best. or do N^2. must pad each list with 0s.
}
